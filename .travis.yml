# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: false
cache: packages

matrix:
  include:
  - r: devel
  - r: release
  - r: oldrel
  - r: 3.2
  - r: 3.1
